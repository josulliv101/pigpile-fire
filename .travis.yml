language: node_js
node_js:
  - "6.11.1"
env:
  global:
    - NODE_ENV=production
script:
  - echo "Deploying"
install:
  - yarn install -g firebase-tools
  - yarn --production=false install && yarn run build && cd functions && yarn install
after_success:
  - firebase use default
  - firebase deploy --token $FIREBASE_TOKEN